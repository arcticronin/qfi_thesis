\chapter{Quantum Metrology under Noise}
\label{ch:noise_analysis}

This chapter studies this question: if an $N$-qubit probe encodes the transverse field $h_x$, how much information about $h_x$ remains when an observer can access only $n$ qubits?  The observer's state is obtained by discarding the rest of the probe.

The central issue is \emph{local metrology}.  Quantum Fisher information (QFI) measures how rapidly a parameterized quantum state changes when $h_x$ changes: more local QFI means that, in principle, measurements on $S$ can estimate $h_x$ more precisely.  We are intersted in measuring how much QFI is lost by restricted access, and whether the answer depends on \emph{how the full probe was prepared} before $E$ was discarded.

The calculations follow Chapter~\ref{ch:methods}.  We evaluate QFI using the formalism of Chapter~\ref{ch:MathBackground}, and use the finite-displacement fidelity quantities from Section~\ref{sec:bounds} as complementary diagnostics.  The preparation history matters: a ground state, a globally Gibbs-prepared state, and a depolarized state can all give mixed states on $S$, but they are not the same physical experiment.

\begin{remark}[This chapter takeaways]
    For the finite TFIM geometries, field windows, and state families studied here, trivially more accessible qubits leave more local QFI.  Depolarization provides a simple baseline and lowers local QFI monotonically in the sampled sweep.  In selected regimes, global Gibbs preparation leaves more local QFI than the reduced ground-state family.  This does not mean that a fixed thermal-noise channel increases QFI, nor does it establish a universal scaling law or a general benefit of temperature.
\end{remark}
% ==============================================================================
\section{The Problem and the Three Preparations}
\label{sec:noise_state_families}
% ==============================================================================
\subsection{Accessible and inaccessible qubits}

Let $S$ denote the accessible $n$-qubit subsystem and $E$ the inaccessible complement of $N-n$ qubits\footnote{Standard convention in quantum information, $S$ stands for State, the one accessible to the observer and $E$ for the environment or the part that is traced out.}.  The parameter $h_x$ serves as the estimation parameter $\theta$ of Chapters~\ref{ch:MathBackground} and~\ref{ch:methods}.  In all scenarios, restricted access is modeled by taking the partial trace $\operatorname{Tr}_E$ over $E$.  Here, \emph{local} means that only the subsystem $S$ is accessible; it does not restrict the observer to product measurements on the qubits within $S$.

\begin{infobox}{What is being compared}
    Every comparison starts from the same parameter-dependent TFIM Hamiltonian $H_N(h_x)$ and retains the same subsystem $S$.  What changes is how the full probe is prepared before the partial trace:
    \begin{enumerate}
        \item \textbf{Ground-state baseline.}  Prepare the full probe in its ground state and trace out $E$.  The resulting reduced state of $S$ can already be mixed, because $S$ and $E$ may be entangled.
        \item \textbf{Depolarizing control.}  Prepare the ground state, apply a fixed depolarizing channel to the full probe, and then trace out $E$.  This is the only fixed, parameter-independent noise channel considered here.
        \item \textbf{Thermal family.}  Prepare the full probe in the Gibbs state at fixed inverse temperature $\beta$, and then trace out $E$.  This is a distinct parameter-dependent preparation family, not a fixed channel applied to the ground-state family.
    \end{enumerate}
    An important note is that the main comparison is not ``the same state with two kinds of noise.''  It is a comparison of three reduced states produced by three distinct physical histories.  Secondly, the traced-out subsystem $E$ is not the external bath used to prepare a Gibbs state, it is simply the finite portion of the probe to which the observer has no local access.
\end{infobox}

\begin{definition}[Prepared and reduced state families]
    The ground-state reference, depolarized, and thermal families are
    \begin{align}
        \rho_{\mathrm{G},N}(h_x)
         & \coloneqq |\psi_0(h_x)\rangle\langle\psi_0(h_x)|,              &
        \rho_{\mathrm{G},S}(h_x)
         & \coloneqq \operatorname{Tr}_E[\rho_{\mathrm{G},N}(h_x)]          \\
        \rho_{\mathrm{dep},N}(h_x;P)
         & \coloneqq \Lambda^{(N)}_P[\rho_{\mathrm{G},N}(h_x)],           &
        \rho_{\mathrm{dep},S}(h_x;P)
         & \coloneqq \operatorname{Tr}_E[\rho_{\mathrm{dep},N}(h_x;P)]      \\
        \rho_{\mathrm{th},N}(h_x;\beta)
         & \coloneqq \frac{\exp[-\beta H_N(h_x)]}{Z(h_x,\beta)},          &
        Z(h_x,\beta)
         & \coloneqq \operatorname{Tr}[\exp[-\beta H_N(h_x)]]               \\
        \rho_{\mathrm{th},S}(h_x;\beta)
         & \coloneqq \operatorname{Tr}_E[\rho_{\mathrm{th},N}(h_x;\beta)]
        \label{eq:noise_state_families}
    \end{align}
    Here $\Lambda^{(N)}_P$ is a full-system depolarizing channel with fixed probability $P$, and $\beta$ is the inverse temperature of the Gibbs preparation: larger $\beta$ means a colder Gibbs state.  When the ground state is nondegenerate, the Gibbs family approaches the ground-state reference as $\beta\to\infty$; at a degeneracy, it instead approaches the normalized projector onto the ground-state subspace.
\end{definition}

The three primary local states are $\rho_{\mathrm{G},S}$, $\rho_{\mathrm{dep},S}$, and $\rho_{\mathrm{th},S}$.  They have the same accessible Hilbert-space dimension, but not the same spectrum or dependence on $h_x$.  The corresponding full-system curves are reference ceilings and consistency checks; the physical question is always what an observer restricted to $S$ can learn.

Related work has studied the estimation of Hamiltonian parameters from Gibbs states at known inverse temperature~\cite{Garcia2024}.  Here, the additional question is how much of that information remains after access is restricted to $S$.

\todo[]{insert diagram 1}

\noindent\emph{Diagram brief.} Project~1 baseline:
\[
    h_x\longrightarrow H_N(h_x)\longrightarrow |\psi_0(h_x)\rangle
    \longrightarrow \rho_{\mathrm{G},N}(h_x)
    \xrightarrow{\operatorname{Tr}_E}\rho_{\mathrm{G},S}(h_x)
    \longrightarrow \text{local SLD QFI and TQFI}.
\]
There is no external noise map. The subsystem is mixed because it is correlated with the discarded degrees of freedom.

\todo[]{insert diagram 2}

\noindent\emph{Diagram brief.} Project~9 depolarizing branch:
\[
    \rho_{\mathrm{G},N}(h_x)
    \xrightarrow{\Lambda_P^{(N)}}\rho_{\mathrm{dep},N}(h_x;P)
    \xrightarrow{\operatorname{Tr}_E}\rho_{\mathrm{dep},S}(h_x;P)
    \longrightarrow \text{local SLD QFI/TQFI}.
\]
The channel is parameter independent when $P$ is fixed.

\todo[]{insert diagram 3}

\noindent\emph{Diagram brief.} Thermal branch:
\[
    h_x\longrightarrow H_N(h_x)\longrightarrow\rho_{\mathrm{th},N}(h_x;\beta)
    \xrightarrow{\operatorname{Tr}_E}\rho_{\mathrm{th},S}(h_x;\beta)
    \longrightarrow \text{local SLD QFI/TQFI}.
\]
The diagram should state explicitly that both $H_N$ and the Gibbs state depend on $h_x$.


\begin{infobox}{Scope of the Thermal Model}
    The thermal branch does not model dynamical thermalization of the accessible subsystem by the discarded qubits. Instead, we assume that the full finite TFIM probe has been prepared in the Gibbs state
    \[
        \rho_{\mathrm{th},N}(h_x;\beta)
        =
        \frac{e^{-\beta H_N(h_x)}}{Z(h_x,\beta)}
    \]
    by an external, unmodelled equilibrium-preparation mechanism. We then restrict access to the subsystem $S$ by tracing out the complementary degrees of freedom $E$.

    Thus, $E$ represents an inaccessible part of the prepared probe, not a macroscopic thermal bath. In general,
    \[
        \operatorname{Tr}_E[\rho_{\mathrm{th},N}]
        \neq
        \frac{e^{-\beta H_S}}{\operatorname{Tr}[e^{-\beta H_S}]}
    \]
    because interactions across the $S$--$E$ boundary and their correlations remain encoded in the reduced state. Our model  studies the local metrological consequences of global thermal preparation, rather than a microscopic thermalization dynamics.
\end{infobox}


\subsection{Which comparisons are valid?}

For a fixed $P$, the depolarizing channel and the partial trace are parameter-independent channels.  QFI cannot increase under either operation, giving
\begin{equation}
    \mathcal{F}_Q[\rho_{\mathrm{dep},S}(h_x;P)]
    \leq \mathcal{F}_Q[\rho_{\mathrm{dep},N}(h_x;P)]
    \leq \mathcal{F}_Q[\rho_{\mathrm{G},N}(h_x)].
    \label{eq:depolarizing_data_processing}
\end{equation}
Likewise, restricted access cannot increase the QFI within the ground-state and thermal families:
\begin{equation}
    \mathcal{F}_Q[\rho_{\mathrm{G},S}(h_x)]
    \leq \mathcal{F}_Q[\rho_{\mathrm{G},N}(h_x)],
    \qquad
    \mathcal{F}_Q[\rho_{\mathrm{th},S}(h_x;\beta)]
    \leq \mathcal{F}_Q[\rho_{\mathrm{th},N}(h_x;\beta)].
    \label{eq:local_global_data_processing}
\end{equation}

These inequalities compare a full probe and a reduced probe \emph{within the same preparation family}.  They do not imply
\begin{equation}
    \mathcal{F}_Q[\rho_{\mathrm{th},S}(h_x;\beta)]
    \leq
    \mathcal{F}_Q[\rho_{\mathrm{G},S}(h_x)]
    \qquad
    \text{(not guaranteed)}
    \label{eq:no_cross_family_qfi_ordering}
\end{equation}
At fixed $\beta$, Gibbs preparation defines a different $h_x$-dependent family, not a parameter-independent channel applied to the ground state.  The thermal and reduced-ground-state QFIs therefore have no universal ordering.  A thermal--ground comparison is consistent with data processing provided that the thermal local QFI remains below that of its full thermal parent.

\subsection{Fixed controls and matched purity}

Most results keep the physical control fixed while sweeping $h_x$ :
\begin{itemize}
    \item The depolarizing branch is a fixed noise channel with depolarizing probability $P$ (probability of obtaining the maximally mixed state $\frac{1}{2^N} \cdot \mathbb{I}$)
    \item The thermal branch fixes the inverse temperature $\beta$ used to prepare the Gibbs family.  These are the primary physical comparisons.
\end{itemize}

A separate control, used only in one of the experiments, matches the local purities at every field value:
\begin{equation}
    \operatorname{Tr}[\rho_{\mathrm{dep},S}^2(h_x;P(h_x))]
    =\operatorname{Tr}[\rho_{\mathrm{G},S}^2(h_x)],
    \qquad
    \operatorname{Tr}[\rho_{\mathrm{th},S}^2(h_x;\beta(h_x))]
    =\operatorname{Tr}[\rho_{\mathrm{G},S}^2(h_x)]
    \label{eq:purity_matching_cond}
\end{equation}

Here, we want to analyze if equal local purity is sufficient to imply equal local QFI.  It is not a fixed-channel experiment: $P$ and $\beta$ are selected as functions of $h_x$.  It therefore compares selected paths through state space rather than one apparatus operated at fixed settings.

\todo[]{insert diagram 4}

\noindent\emph{Diagram brief.} Show $\rho_{\mathrm{G},S}(h_x)$ setting a target purity, then a choice of $P(h_x)$ and $\beta(h_x)$ before reduction to equal-purity local comparison families.

\begin{infobox}{Why matched-purity QFI requires care}
    The matching settings $P$ and $\beta$ are chosen separately at every value of $h_x$.  The corresponding QFI must therefore follow the resulting curve through state space, rather than differentiate a state at fixed $P$ or fixed $\beta$.  In general, if $\rho$ depends on a setting $\lambda(h_x)$, then
    \begin{equation}
        \frac{\mathrm{d}\rho}{\mathrm{d}h_x}
        =
        \frac{\partial\rho}{\partial h_x}
        +
        \frac{\partial\rho}{\partial\lambda}
        \frac{\mathrm{d}\lambda}{\mathrm{d}h_x}
        \label{eq:matched_path_total_derivative}
    \end{equation}
    Here $\lambda$ is $P$ or $\beta$.  Omitting the second term would calculate the QFI of a different family.  Because $P$ or $\beta$ now depends on $h_x$, the fixed-channel comparison with the ground-state reference does not apply to these matched paths.  The Project~9 calculations include the total-derivative terms; the branch-specific expressions are recorded in Appendix~\ref{app:matched_purity_derivatives}.
\end{infobox}

% ==============================================================================
\section{What Is Calculated and Compared}
\label{sec:noise_observables}
% ==============================================================================

For each $h_x$, the calculation constructs a state family, discards $E$, and asks how strongly the resulting local state changes.  We also evaluate fidelity quantities from $\rho(h_x)$ and $\rho(h_x+\delta h_x)$, because these are the quantities accessible to a truncation-based metrology protocol.  The calculation and central-difference convention follow Chapter~\ref{ch:methods}, the finite-displacement functional is Eq.~\eqref{eq:qfi_difference_quotient}.

\begin{remark}[Exact QFI and TQFI serve different purposes]
    Exact SLD QFI is the local, infinitesimal benchmark that supports the physical claims in this chapter.  TQFI is a finite-displacement, truncation-aware proxy.
    % At the selected displacements, \texttt{reduced\_lower\_tqfi} can exceed exact local SLD QFI: the largest observed excesses are $0.112$ in the latest Project~1 size sweep, $1.068$ in the Project~1 subsystem-size sweep, and $1.071$ in the open-boundary Project~7 data.  In contrast, \texttt{reduced\_sub\_qfi\_bound} never exceeds local SLD QFI in the current arrays.  TQFI is therefore a useful diagnostic, not a substitute for exact local SLD QFI.
    % For reproducibility, the saved \texttt{.npz} outputs were generated between 2026-08-22 and 2026-08-25: \texttt{project1\_scaling\_N\_20260822\_162213.npz} and \texttt{project1\_scaling\_n\_20260822\_162301.npz}, six Project~7 outputs dated 2026-08-23, and twenty-one Project~9 outputs dated 2026-08-25.  Projects~1 and~7 use $\delta h_x=0.1$ and $m=2$; Project~9 uses $\delta h_x=0.05$ and normally $m=3$.  
    Exact SLD values may be compared when the physical families agree, but raw TQFI magnitudes should not be compared across projects without accounting for the different displacement and truncation settings.
\end{remark}

The results are presented in three steps.  First, we establish the loss caused by losing some qubit.  Then we examine the thermal family and its temperature dependence.  Finally, we compare the three preparation histories at fixed accessible size.
% ==============================================================================
\section{Restricted Access Alone}
\label{sec:project1_noise}
% ==============================================================================

The first calculation is the reference case.  The full probe is prepared in its ground state, no external noise channel is applied, and only then is $E$ discarded.  This isolates the information loss caused by measuring $S$.

\subsection{Changing total and accessible size}

The open-chain sequence $(N,n)=(4,2),(6,4),(8,6)$ changes both total size and accessible fraction.

\begin{table}[htbp]
    \centering
    \caption{Project~1 open-chain scaling sequence with $n=N-2$.}
    \label{tab:project1_size_sequence}
    \begin{tabularx}{\textwidth}{
            c
            >{\centering\arraybackslash}X
            >{\centering\arraybackslash}X
            >{\centering\arraybackslash}X
            >{\centering\arraybackslash}X
            >{\centering\arraybackslash}X
        }
        \toprule
        \thead{$(N,n)$} &
        \thead{Peak local                \\ SLD} &
        \thead{Peak full                 \\ SLD} &
        \thead{Local /                   \\ full} &
        \thead{Peak                      \\ lower TQFI } &
        \thead{Peak                      \\ sub QFI} \\
        \midrule
        $(4,2)$         & \thead{$2.859$ \\at $0.508$ }& \thead{$4.393$ \\at $0.427$ } & $0.651$ & $1.963$ & $1.419$ \\
        $(6,4)$         & \thead{$5.919$ \\at $0.631$} & \thead{$6.780$ \\at $0.631$ } & $0.873$ & $3.164$ & $1.408$ \\
        $(8,6)$         & \thead{$9.472$ \\at $0.712$ }& \thead{$10.062$\\ at $0.712$} & $0.941$ & $4.440$ & $1.326$ \\
        \bottomrule
    \end{tabularx}
\end{table}

\begin{figure}[H]%[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/noise/project1/proj1_fig_1_1.png}
    \caption{Project~1 main-text figure P1.1: local bounds and exact local SLD in small multiples for the three rows of Table~\ref{tab:project1_size_sequence}.}
    \label{fig:project1_fig_1_1}
\end{figure}


The local peak rises strongly, while the retained fraction of the full peak increases from about $65\%$ to $94\%$. Intuitively, the discarded environment remains two qubits while the accessible subsystem grows.
Partial trace is the only loss mechanism. We can also see both that the local peak and the full-system peak shift in location towards the theoretical critical point for the phase transition ($h_x$ = 1).

\subsection{Retaining more qubits at fixed total size}

\begin{table}[H]%[htbp]
    \centering
    \caption{Project~1 recovery of the full-system peak as the accessible subsystem grows at fixed open-chain size $N=8$.}
    \label{tab:project1_subsystem_sequence}
    \begin{tabular}{c c c}
        \toprule
        $n$ & Peak local SLD QFI & Fraction of full SLD peak \\
        \midrule
        $2$ & $3.247$            & $0.323$                   \\
        $3$ & $5.154$            & $0.512$                   \\
        $4$ & $6.932$            & $0.689$                   \\
        $5$ & $8.389$            & $0.834$                   \\
        $6$ & $9.472$            & $0.941$                   \\
        $7$ & $10.062$           & $1.000$                   \\
        \bottomrule
    \end{tabular}
\end{table}

Retaining more qubits gives rapid, monotonic recovery of the critical response. For this finite system, tracing out one qubit leaves the peak location unchanged, and the location of the local maximum approaches the full-system maximum as $n$ increases.

\todo[]{insert diagram 6}

\noindent\emph{Plot brief.} Insert Project~1 main-text figure P1.2: exact local-SLD overlays for $n=2,\ldots,7$ at fixed $N=8$, Fig~\ref{fig:project1_fig_1_2}.

\begin{Todo}
    Pick one
\end{Todo}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.43\textwidth, valign=c]{figures/noise/project1/subsystem_SLD_varying_n_fixing_N8.png}\hfill
    \includegraphics[width=0.53\textwidth, valign=c]{figures/noise/project1/local_bounds_and_exact_over_n_subsystem.png}
    \caption{Project~1 main-text figure P1.3: local bounds and exact over $n$}
    \label{fig:project1_fig_1_3}
\end{figure}



\todo[]{insert diagram 7}

\noindent\emph{Supplementary plot brief.} Insert separate heatmaps for local SLD, \texttt{lower\_tqfi}, and \texttt{sub\_qfi\_bound} across the $n$ sweep Fig~\ref{fig:project1_fig_1_2_heatmaps}.


\begin{figure}[H]%[htbp]
    \centering
    \includegraphics[width=0.48\textwidth]{figures/noise/project1/subsystem_SLD_varying_n_fixing_N8_heatmap.png}
    \includegraphics[width=0.48\textwidth]{figures/noise/project1/subsystem_TQFI_varying_n_fixing_N8_heatmap.png}

    \caption{Project~1 main-text figure P1.2 heatmaps}
    \label{fig:project1_fig_1_2_heatmaps}
\end{figure}

\begin{figure}[H]%[htbp]
    \centering
    \includegraphics[width=1\textwidth]{figures/noise/project1/local_bounds_and_exact_over_n_subsystem.png}
    \caption{Project~1 main-text figure P1.3: local bounds and exact over $n$}
    \label{fig:project1_fig_1_3}
\end{figure}





At the local-SLD peak, the \texttt{lower\_tqfi}/SLD ratio ranges from about $0.42$ to $0.96$ in the two Project~1 sweeps, the corresponding \texttt{sub\_qfi\_bound}/SLD ratio ranges from about $0.05$ to $0.60$. The finite-displacement quantities are informative but can be loose when the reduced state is strongly mixed or the truncation misses part of the response.
% ==============================================================================
\section{Global Thermal Preparation}
\label{sec:project7_noise}
% ==============================================================================

I next map the local response of the globally Gibbs-prepared family as its inverse temperature and finite-system geometry are varied.  For every $h_x$, the whole $N$-qubit probe is prepared at a chosen $\beta$ before the same subsystem $S$ is retained.  This establishes the temperature and boundary-condition dependence used in the direct comparison below.

Related full-probe work has reported temperature-enhanced QFI in critical models~\cite{osterma2024}, lending context to the temperature-dependent behaviour found here.

The sampled inverse temperatures are
\[
    \beta=0.1,\ 0.25,\ 0.5,\ 1,\ 2,\ 5,\ 10,\ 25,\ 50.
\]
Larger $\beta$ corresponds to a colder Gibbs state.  For a compact numerical summary, I define $\beta_\star$ as the interpolated value at which the peak \texttt{reduced\_lower\_tqfi} reaches $90\%$ of the ground-state \texttt{lower\_tqfi} peak.  It is a convenient finite-displacement recovery marker, not a thermodynamic transition temperature.

\subsection{Temperature dependence in an open chain}

For $N=6$, $n=4$, and open boundaries:
\begin{table}[htbp]
    \centering
    \caption{Project~7 open-chain local recovery.}
    \label{tab:project7_open_recovery}
    \begin{tabular}{c c c c}
        \toprule
        State        & Peak \texttt{lower\_tqfi} & Peak local SLD & Peak full SLD \\
        \midrule
        Ground state & $5.019$                   & $6.014$        & $6.782$       \\
        $\beta=1$    & $0.747$                   & $1.714$        & $2.730$       \\
        $\beta=2$    & $1.440$                   & $2.792$        & $4.292$       \\
        $\beta=5$    & $3.987$                   & $6.151$        & $9.578$       \\
        $\beta=10$   & $6.164$                   & $8.962$        & $16.916$      \\
        \bottomrule
    \end{tabular}
\end{table}

For this run, $\beta_\star=6.216$.  As the Gibbs state is cooled, the local thermal response grows and its dominant feature moves from the edge of the scan toward the finite-size critical region.  This describes the observed curve; it does not mean that a fixed noisy channel reverses its own information loss.

\begin{infobox}{Why the thermal response can rise}
    At finite temperature, QFI can receive contributions both from changing eigenvectors and from the field-dependent populations of low-lying excitations.  Around the open-chain peak at $h_x=0.547$, the first excitation gap is approximately $0.038$.  Even $\beta=50$ is therefore not uniformly in the ground-state regime over the whole scan.  This is a finite-system temperature effect; it does not mean that the Gibbs state is more informative than the ground state at every fixed field as $\beta\rightarrow\infty$.
\end{infobox}

The figure source is \texttt{/home/ronin/Dev/space/QCES/cqes/project7\_plots.ipynb}.

\todo[]{insert diagram 8}


\noindent\emph{Plot brief.} Insert Project~7 figure P7.1: recovery of peak local information versus $\beta$, including the $90\%$ reference used for $\beta_\star$.


\begin{figure}[H]%[htbp]
    \centering
    \includegraphics[width=1\textwidth]{figures/noise/project7/recovery_peak_vs_beta.png}
    \caption{Project~7 main-text figure P7.1: recovery of peak local information versus $\beta$}
    \label{fig:project7_fig_7_1}
\end{figure}

\todo[]{insert diagram 9}

\noindent\emph{Plot brief.} Insert Project~7 figure P7.2: ground-state local bounds and local SLD with the thermal \texttt{lower\_tqfi} overlay, showing peak migration.

\subsection{Changing geometry and boundary conditions}

\begin{table}[htbp]
    \centering
    \caption{Project~7 operational recovery scales.}
    \label{tab:project7_beta_star}
    \begin{tabular}{c c}
        \toprule
        Geometry                    & $\beta_\star$ from peak \texttt{lower\_tqfi} \\
        \midrule
        Open, $N=4$, $n=2$          & $5.170$                                      \\
        Open, $N=6$, $n=4$          & $6.216$                                      \\
        Periodic, $N=6$, $n=4$      & $3.122$                                      \\
        Anti-periodic, $N=6$, $n=4$ & $0.517$                                      \\
        \bottomrule
    \end{tabular}
\end{table}

The larger sampled open chain needs a somewhat colder Gibbs state for the selected threshold. Boundary conditions have a greater effect: periodic boundaries recover at lower $\beta_\star$ than open ones, and anti-periodic boundaries qualitatively reshape the low-energy sector. In the anti-periodic run, the local thermal maximum lies at the lower scan boundary $h_x=0.1$, where the first two gaps are approximately $0.0295$. This is evidence of strong low-field susceptibility in the sampled window, not yet a bulk critical claim.

\todo[]{insert diagram 10}

\noindent\emph{Plot brief.} Insert Project~7 figure P7.3: $\beta_\star$ across boundary conditions, framed as a spectral-control comparison rather than a phase diagram.

\todo[]{insert diagram 11}

\noindent\emph{Supplementary plot brief.} Insert separate local-SLD and TQFI-tightness heatmaps across $\beta$.

% ==============================================================================
\section{Comparing the Three Preparation Histories}
\label{sec:project9_noise}
% ==============================================================================

This is the main controlled comparison.  It places $\rho_{\mathrm{G},S}$, $\rho_{\mathrm{dep},S}$, and $\rho_{\mathrm{th},S}$ on equal $n$-qubit footing: the observer retains the same size subsystem while the full probe is prepared in different ways.  Exact local SLD QFI is the primary output; the full-system curves serve only as the consistency checks of Eqs.~\eqref{eq:depolarizing_data_processing} and~\eqref{eq:local_global_data_processing}.  All data sets analysed here satisfy these checks.

The five comparisons answer different questions.  The first checks the matched-purity construction; the second asks how the three local families change as access to the probe grows; the third changes the boundary-condition-dependent spectrum; the fourth isolates the fixed depolarizing channel; and the fifth is the central temperature sweep of the globally Gibbs-prepared family.

\subsection{Matched-purity control}

\begin{table}[htbp]
    \centering
    \caption{Project~9 matched-purity control.}
    \label{tab:project9_preset_a}
    \begin{tabular}{c c c c}
        \toprule
        $(N,n)$ & Peak pure SLD & Peak subsystem SLD & Subsystem/pure ratio \\
        \midrule
        $(4,2)$ & $4.388$       & $3.215$            & $0.733$              \\
        $(6,4)$ & $6.771$       & $6.013$            & $0.888$              \\
        $(8,6)$ & $10.057$      & $9.436$            & $0.938$              \\
        \bottomrule
    \end{tabular}
\end{table}

This control confirms the local-scaling trend found above, but it is not the main channel-separation result.  Purity matching drives $P_{\mathrm{matched}}$ to approximately $4\times10^{-9}$--$1.5\times10^{-8}$ and $\beta_{\mathrm{matched}}$ to $500$--$1.2\times10^5$, so the three local families coincide to numerical precision.  The matched-family derivative and reduction are consistent in this near-pure regime, but this does not show that entanglement-induced, depolarizing, and thermal mixedness are generally equivalent at equal purity.

The figure source is \texttt{/home/ronin/Dev/space/QCES/cqes/project9\_plots.ipynb}.

\todo[]{insert diagram 12}

\noindent\emph{Supplementary plot brief.} Insert Project~9 figure P9.S1: matched-purity curves together with $P_{\mathrm{matched}}$ and $\beta_{\mathrm{matched}}$ metadata.

\subsection{Accessible subsystem size}

For $N=6$ with periodic boundaries:
\begin{table}[htbp]
    \centering
    \caption{Project~9 local SLD peaks versus accessible subsystem size at fixed preparation controls.}
    \label{tab:project9_preset_b}
    \begin{tabular}{c c c c}
        \toprule
        $n$ & $\rho_{\mathrm{G},S}$ & $\rho_{\mathrm{dep},S}$ & $\rho_{\mathrm{th},S}$ \\
        \midrule
        $1$ & $0.972$               & $0.722$                 & $0.348$                \\
        $2$ & $2.134$               & $1.724$                 & $0.688$                \\
        $3$ & $3.164$               & $2.672$                 & $1.040$                \\
        $4$ & $3.926$               & $3.419$                 & $1.393$                \\
        $5$ & $4.344$               & $3.865$                 & $1.741$                \\
        \bottomrule
    \end{tabular}
\end{table}

At the principal peak in this $\beta=1$ baseline,
\[
    \mathcal{F}_Q[\rho_{\mathrm{G},S}]\geq
    \mathcal{F}_Q[\rho_{\mathrm{dep},S}]>
    \mathcal{F}_Q[\rho_{\mathrm{th},S}].
\]
Peak ordering is not the full field-resolved story.  In every $n$ run, the thermal family exceeds the reduced ground-state family over broad off-peak intervals; for $n=4$, the largest pointwise thermal advantage is $0.456$ at $h_x=1.686$.  A state family can be inferior by maximum QFI while being superior over a useful interval of the operating field, which is why the chapter reports both peak tables and field-resolved plots.

\todo[]{insert diagram 13}

\noindent\emph{Plot brief.} Insert Project~9 figure P9.1: peak local SLD QFI versus $n$ for the three local families.

\subsection{Boundary conditions}

\begin{table}[htbp]
    \centering
    \caption{Project~9 local SLD peaks at fixed $N=6$ and $n=4$.}
    \label{tab:project9_preset_c}
    \begin{tabular}{c c c c}
        \toprule
        Boundary condition & $\rho_{\mathrm{G},S}$ & $\rho_{\mathrm{dep},S}$ & $\rho_{\mathrm{th},S}$ \\
        \midrule
        Open               & $6.014$               & $5.287$                 & $1.714$                \\
        Periodic           & $3.926$               & $3.419$                 & $1.393$                \\
        Anti-periodic      & $1.293$               & $1.096$                 & $1.892$                \\
        \bottomrule
    \end{tabular}
\end{table}

The anti-periodic branch is the clearest thermal-advantage example: the thermal family exceeds the reduced ground-state family at all $60/60$ sampled fields and has the larger peak, $1.892$ versus $1.293$. The thermal maximum is at the lower scan boundary $h_x=0.1$. The supported statement is that boundary-condition-dependent spectral structure changes the local channel hierarchy in the sampled window, it is not a bulk critical conclusion.

\todo[]{insert diagram 14}

\noindent\emph{Plot brief.} Insert Project~9 figure P9.5: field-resolved open, periodic, and anti-periodic panels. The anti-periodic panel should make the hierarchy reversal explicit.

\subsection{Depolarizing strength}

For $N=6$, $n=4$, periodic boundaries, and varying only $P$:
\begin{table}[htbp]
    \centering
    \caption{Project~9 depolarizing baseline.}
    \label{tab:project9_preset_d}
    \begin{tabular}{c c c}
        \toprule
        $P$    & Peak $\mathcal{F}_Q[\rho_{\mathrm{dep},S}]$ & Change from $P=0$ \\
        \midrule
        $0.00$ & $3.926$                                     & $0\%$             \\
        $0.05$ & $3.661$                                     & $-6.7\%$          \\
        $0.10$ & $3.419$                                     & $-12.9\%$         \\
        $0.20$ & $2.953$                                     & $-24.8\%$         \\
        $0.30$ & $2.499$                                     & $-36.4\%$         \\
        \bottomrule
    \end{tabular}
\end{table}

Depolarization lowers local QFI smoothly and monotonically.  The ground-subsystem and thermal curves remain unchanged in this sweep because only $P$ is varied.

\todo[]{insert diagram 15}

\noindent\emph{Plot brief.} Insert Project~9 figure P9.2: peak local SLD QFI versus depolarizing probability $P$.

\subsection{Temperature sweep at fixed accessible size}

For $N=6$, $n=4$, and periodic boundaries, the depolarized comparator uses fixed $P=0.1$ while the thermal branch varies $\beta$:
\begin{table}[htbp]
    \centering
    \caption{Project~9 thermal sweep. The ground-subsystem peak is $3.926$.}
    \label{tab:project9_preset_e}
    \begin{tabular}{c c c c}
        \toprule
        $\beta$ & Peak $\mathcal{F}_Q[\rho_{\mathrm{th},S}]$ & Peak field & Relation to ground subsystem             \\
        \midrule
        $0.2$   & $0.152$                                    & $0.100$    & strongly suppressed                      \\
        $0.5$   & $0.729$                                    & $0.100$    & suppressed                               \\
        $1.0$   & $1.393$                                    & $0.792$    & suppressed                               \\
        $2.0$   & $2.607$                                    & $1.076$    & lower peak, pointwise crossings occur    \\
        $5.0$   & $4.847$                                    & $1.036$    & $23.5\%$ above the ground-subsystem peak \\
        \bottomrule
    \end{tabular}
\end{table}

At $\beta=5$, the thermal local QFI is greater than the reduced-ground-state local QFI at $40/60$ sampled fields. It remains bounded by its full thermal parent:
\begin{equation}
    \max_{h_x}\mathcal{F}_Q[\rho_{\mathrm{th},S}]
    =4.847
    <
    8.456
    =\max_{h_x}\mathcal{F}_Q[\rho_{\mathrm{th},N}].
    \label{eq:project9_thermal_consistency}
\end{equation}
The finite-system mechanism is the field dependence of low-lying thermal populations, not an increase of QFI under a fixed channel.

For the finite TFIM geometries and field windows studied here, thermal preparation before partial trace can enhance local SLD QFI relative to the reduced ground-state subsystem.  The effect depends on the regime, is strongest at larger $\beta$ in the present sweep, and remains bounded by the QFI of the full thermal family.

\todo[]{insert diagram 16}

\noindent\emph{Plot brief.} Insert Project~9 figure P9.3: peak local SLD QFI versus $\beta$, with a companion panel or table for the peak-field location.

\todo[]{insert diagram 17}

\noindent\emph{Plot brief.} Insert Project~9 figure P9.4: field-resolved curves at $\beta=5$ for the reduced ground-state, depolarized, and thermal families.

\todo[]{insert diagram 18}

\noindent\emph{Supplementary plot brief.} Insert Project~9 figure P9.S2: a compact audit of the three full-to-local SLD-QFI inequalities at every sampled field.

% ==============================================================================
\section{What the Calculations Show}
\label{sec:noise_synthesis}
% ==============================================================================

\begin{infobox}{The central comparison}
    Restricted access reduces QFI within each preparation family.  Across different preparation histories, however, the local QFI need not have the same ordering: depolarization lowers it in the present control sweep, whereas global Gibbs preparation can yield a locally more informative state than the reduced ground-state family in selected finite-system regimes.  This is a comparison between distinct encoded families, not an increase of QFI under one fixed thermal-noise channel.
\end{infobox}

\subsection{Three conclusions}

Critical metrological response is spatially distributed.  Partial trace removes a variable fraction of it, and the retained fraction grows strongly with accessible subsystem size.  Keeping all but one qubit recovers essentially all of the full-system peak QFI in the tested $N=8$ open-chain and $N=6$ periodic cases.

Entanglement-induced mixedness, depolarization, and thermal population are not interchangeable.  Purity alone does not predict local QFI: the field dependence of the state and the part of the spectrum populated by the preparation both matter.

Boundary conditions change finite-size gaps, low-energy sectors, peak positions, and thermal recovery scales.  In the present systems, they can reverse the ordering between reduced ground-state and reduced thermal families.

\subsection{What these results do not establish}

The present data do not support a universal QFI scaling exponent.  Thermal preparation is not generically beneficial and should never be described as a fixed channel increasing QFI of the same encoded family.  Local TQFI should not be compared directly with global SLD QFI, and \texttt{lower\_tqfi} should not be called a strict pointwise SLD lower bound at the present finite displacements.  Finally, a peak at the edge of the field scan is a reason to validate the scan window, not a final peak location.

\subsection{A methodological lesson}

Exact local SLD QFI is the correct benchmark where it can be computed.  Finite-displacement TQFI adds a complementary operational layer by testing whether a truncated or fidelity-based description retains the same feature.  It should be validated against exact local SLD on small systems before it is used where exact SLD is unavailable.

\subsection{Limitations and outlook}

The present data establish finite-system trends rather than asymptotic laws.  A controlled size study, performed separately at fixed accessible size $n$ and fixed retained fraction $n/N$, is required before assigning scaling exponents to peak heights or locations.  The low-temperature limit should likewise be checked through pointwise convergence of the thermal family to the reduced ground-state family and through more than one well-defined crossover diagnostic.

Several boundary-condition runs have their largest thermal response at the lower edge of the sampled field interval.  Their peak location should therefore be tested with a wider scan, or supported by an analytic argument for the chosen boundary.  Finally, examining the lowest excitation gaps alongside the strongest thermal-enhancement curves would provide a more direct spectral interpretation of the observed hierarchy changes.

For the finite probes studied here, local metrological performance depends on the full sequence that produces the accessible state.  Access to the relevant subsystem, the low-energy spectrum, and the way mixedness is prepared all matter.
