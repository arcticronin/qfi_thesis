\section{Variational Quantum State Eigensolver (VQSE)} \label{sec:vqse_deep_dive}

The VQSE subroutine supplies the dominant eigenspace required by the TQFI bounds. It variationally learns the $m$ largest eigenvalues $\{\lambda_i\}_{i=1}^m$ and prepares the corresponding eigenbasis $\{|\lambda_i\rangle\}_{i=1}^m$ of an unknown $n$-qubit density matrix $\rho$~\cite{cerezo2022vqse}. In contrast, VQE targets the ground state of a known Hamiltonian.
% ==============================================================================
\subsection{Mathematical Foundation: Diagonalization via Majorization}
% ==============================================================================

Rather than explicitly measuring off-diagonal matrix elements to quantify how far a state is from diagonal form, VQSE exploits the mathematical equivalence between matrix diagonalization and \textbf{quantum majorization}.

\begin{definition}[Quantum Majorization]
    Let $\rho$ be an $n$-qubit density matrix with sorted, non-increasing eigenvalues $\boldsymbol{\lambda} = (\lambda_1, \lambda_2, \dots, \lambda_{2^n})$ where $\lambda_1 \ge \lambda_2 \ge \dots \ge \lambda_{2^n} \ge 0$. Let $\mathbf{p}(\boldsymbol{\beta}) = (p_1, p_2, \dots, p_{2^n})$ denote the diagonal elements of the transformed state $\tilde{\rho} = V(\boldsymbol{\beta})\rho V^\dagger(\boldsymbol{\beta})$ measured in the computational basis $\{|z_k\rangle\}$:
    \begin{equation}
        p_k(\boldsymbol{\beta}) = \langle z_k | V(\boldsymbol{\beta})\rho V^\dagger(\boldsymbol{\beta}) | z_k \rangle.
    \end{equation}
    By the Schur--Horn theorem, the eigenvalue vector $\boldsymbol{\lambda}$ majorizes the diagonal probability vector $\mathbf{p}(\boldsymbol{\beta})$, denoted $\boldsymbol{\lambda} \succ \mathbf{p}(\boldsymbol{\beta})$.
\end{definition}

\begin{theorem}[VQSE Cost Function Minimization]
    Let $H_{\mathrm{VQSE}}$ be an $n$-qubit Hamiltonian diagonal in the computational basis with non-negative, non-degenerate eigenenergies sorted in strictly increasing order $0 \le E_1 < E_2 < \dots < E_{2^n}$. The VQSE cost function is the energy expectation value
    \begin{equation}
        C(\boldsymbol{\beta}) = \operatorname{Tr}\left[ H_{\mathrm{VQSE}} V(\boldsymbol{\beta})\rho V^\dagger(\boldsymbol{\beta}) \right] = \sum_{k=1}^{2^n} E_k p_k(\boldsymbol{\beta}) = \mathbf{E} \cdot \mathbf{p}(\boldsymbol{\beta}).
        \label{eq:vqse_cost_dot_product}
    \end{equation}
    Because the inner product of an increasingly sorted vector $\mathbf{E}$ with a vector $\mathbf{p}$ majorized by $\boldsymbol{\lambda}$ is a Schur-concave function, $C(\boldsymbol{\beta})$ achieves its global minimum when $V(\boldsymbol{\beta}_{\text{opt}})$ maps the $k$-th eigenvector $|\lambda_k\rangle$ of $\rho$ to the $k$-th computational basis state $|z_k\rangle$:
    \begin{equation}
        C_{\text{min}} = \mathbf{E} \cdot \boldsymbol{\lambda} = \sum_{k=1}^{2^n} E_k \lambda_k.
    \end{equation}
\end{theorem}

% ==============================================================================
\subsection{Ansatz Execution and Readout Subroutines}
% ==============================================================================

Once the hybrid quantum-classical loop converges to the optimal parameter set $\boldsymbol{\beta}_{\text{opt}}$, the $m$ dominant eigenvalues and eigenvectors are retrieved as follows:

1. \textbf{Eigenvalue Readout:} Prepare $V(\boldsymbol{\beta}_{\text{opt}}) \rho V^\dagger(\boldsymbol{\beta}_{\text{opt}})$ and sample in the computational basis for $N_{\text{runs}}$ shots. The estimated eigenvalue $\tilde{\lambda}_i$ corresponds to the relative frequency $f_i / N_{\text{runs}}$ of the bitstring $z_i \in \{0,1\}^n$:
\begin{equation}
    \tilde{\lambda}_i^{\text{est}} = \frac{f_i}{N_{\text{runs}}}.
\end{equation}
2. \textbf{Eigenvector Preparation On-Demand:} Given a computational basis state $|z_i\rangle = X^{z_{i,1}} \otimes \dots \otimes X^{z_{i,n}} |0\rangle^{\otimes n}$, the corresponding approximate state eigenvector $|\tilde{\lambda}_i\rangle$ is generated on a quantum register via:
\begin{equation}
    |\tilde{\lambda}_i\rangle = V^\dagger(\boldsymbol{\beta}_{\text{opt}}) |z_i\rangle.
\end{equation}

\begin{algorithm}[H]
    \caption{VQSE Principal Component Extraction}
    \label{alg:vqse}
    \begin{algorithmic}[1]
        \Require Input state $\rho$, truncation rank $m$, parameterized ansatz $V(\boldsymbol{\beta})$, and non-degenerate $H_{\mathrm{VQSE}}$.
        \While{not converged}
        \State Prepare $\rho$, apply $V(\boldsymbol{\beta})$, and measure $C(\boldsymbol{\beta})$.
        \State Estimate $\nabla_{\boldsymbol{\beta}}C$ and update $\boldsymbol{\beta}$ with the selected classical optimizer.
        \EndWhile
        \State Sample $V(\boldsymbol{\beta}_{\mathrm{opt}})\rho V^\dagger(\boldsymbol{\beta}_{\mathrm{opt}})$ in the computational basis.
        \State Estimate the $m$ dominant eigenvalues from the most frequent bitstrings and prepare their eigenvectors with $V^\dagger(\boldsymbol{\beta}_{\mathrm{opt}})$.
    \end{algorithmic}
\end{algorithm}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{asi_report/VQSE_plot.png}
    \caption{VQSE infidelity as a function of the number of ansatz layers.}
    \label{fig:vqse_infidelity_layers}
\end{figure}

% ==============================================================================
\subsection{Adaptive Cost Function Schedule: Mitigating NISQ Landscape Bottlenecks}
% ==============================================================================

The choice of the VQSE target Hamiltonian introduces a fundamental trade-off between trainability and optimization-landscape structure:
\begin{itemize}

    \item \textbf{Fixed Local Hamiltonian ($H_L = \sum_j r_j Z_j$):} Yields local cost functions whose gradients do not exponentially vanish at shallow circuit depths, successfully avoiding barren plateaus. However, strict non-degeneracy restricts the solution space, leading to a high density of sub-optimal local minima.
    \item \textbf{Fixed Global Hamiltonian ($H_G = \sum_{i=1}^m q_i |z_i\rangle\langle z_i|$):} Possesses a $(2^n - m)$-fold degenerate subspace, creating a large, flexible solution space . However, global cost functions exhibit exponentially vanishing gradients (barren plateaus) across random hardware-efficient Ansätze.
\end{itemize}

As an optional optimization extension, we consider a time-dependent, adaptively updated Hamiltonian schedule that interpolates between these two costs:
\begin{equation}
    H_{\mathrm{cost}}(t) = (1 - t) H_L + t H_G(t), \quad t \in [0,1],
    \label{eq:adaptive_hamiltonian}
\end{equation}
where $t = k / N_{\text{max}}$ represents the normalized optimization progress.

\begin{figure}[h!]
    \centering
    \begin{center}
        % Conceptual illustration of adaptive schedule
        \textbf{Adaptive Optimization Dynamics:}
        $t=0 \implies H_{\mathrm{cost}}(0)=H_L$ (local and trainable)
        $\longrightarrow t=1 \implies H_{\mathrm{cost}}(1)=H_G$ (global and highly degenerate)
    \end{center}
    \caption{The proposed adaptive schedule interpolates from a local Hamiltonian $H_L$ to a measurement-informed global Hamiltonian $H_G(t)$, aiming to retain gradient signal while reducing trapping.}
    \label{fig:adaptive_schedule}
\end{figure}

As outlined in Algorithm~\ref{alg:adaptive_vqse}, $H_G(t) = \sum_{i=1}^m q_i |z_i(t)\rangle\langle z_i(t)|$ is periodically re-centered around the dominant bitstrings $z_i(t)$ sampled from intermediate measurements of $V(\boldsymbol{\beta})\rho V^\dagger(\boldsymbol{\beta})$. This dynamic shift changes the cost landscape during training; its effectiveness should be assessed empirically for the chosen ansatz and noise model.

\begin{algorithm}[H]
    \caption{Adaptive VQSE Optimization Schedule}
    \label{alg:adaptive_vqse}
    \begin{algorithmic}[1]
        \Require Input state $\rho$, rank $m$, step size $t_s = 1/s$, local $H_L$, weights $\{q_i\}_{i=1}^m$.
        \State Initialize parameters $\boldsymbol{\beta}_0$ randomly; set $H_{\mathrm{cost}}(0) \leftarrow H_L$, $t \leftarrow \delta t$.
        \While{$t \le 1$}
        \If{$t$ is divisible by $t_s$}
        \State Measure $V(\boldsymbol{\beta})\rho V^\dagger(\boldsymbol{\beta})$ in the computational basis.
        \State Identify $m$ most frequent bitstrings $Z(t) = \{z_1(t), \dots, z_m(t)\}$.
        \State Update global component: $H_G(t) \leftarrow \sum_{i=1}^m q_i |z_i(t)\rangle\langle z_i(t)|$ .
        \EndIf
        \State Update total Hamiltonian: $H_{\mathrm{cost}}(t) \leftarrow (1 - t) H_L + t H_G(t)$.
        \State Execute a classical optimization step: $\boldsymbol{\beta} \leftarrow \text{Optimizer}(\boldsymbol{\beta}, \nabla_{\boldsymbol{\beta}} C(t, \boldsymbol{\beta}))$.
        \State $t \leftarrow t + \delta t$.
        \EndWhile
        \State \textbf{Return:} $\boldsymbol{\beta}_{\text{opt}} \leftarrow \boldsymbol{\beta}$.
    \end{algorithmic}
\end{algorithm}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.8\textwidth]{asi_report/VQSE_Adaptive_Cost_Evolution_8037.png}
    \caption{Observed evolution of the adaptive VQSE cost during optimization.}
    \label{fig:vqse_adaptive_cost}
\end{figure}

% ==============================================================================
\subsection{Finite Shot Sampling Complexity and Verification Bounds}
% ==============================================================================

Evaluating eigenvalues via empirical bitstring frequencies $f_i / N_{\text{runs}}$ introduces statistical sampling noise.

\begin{theorem}[Shot Complexity for Relative Accuracy]
    By Hoeffding's inequality, to ensure that the relative error $\epsilon_i = |\tilde{\lambda}_i^{\text{est}} - \tilde{\lambda}_i| / \tilde{\lambda}_i$ across all $m$ target eigenvalues remains below a threshold $c > 0$ with success probability at least $1 - \delta$, the required number of measurement shots scales as:
    \begin{equation}
        N_{\text{runs}} \ge \frac{\log(1/\delta)}{2 c^2 \lambda_m^2} = \Omega\left( \frac{\log(1/\delta)}{c^2 \lambda_m^2} \right),
        \label{eq:shot_scaling}
    \end{equation}
    where $\lambda_m$ is the smallest eigenvalue within the target truncation rank $m$.
\end{theorem}

\begin{theorem}[Operational Error Verification Bound]
    The total eigenvalue error $\epsilon_\lambda = \sum_{i=1}^m (\lambda_i - \tilde{\lambda}_i)^2$ and eigenvector alignment error $\epsilon_v = \sum_{i=1}^m \langle \delta_i | \delta_i \rangle$ (where $|\delta_i\rangle = (\mathbb{I} - |\tilde{\lambda}_i\rangle\langle\tilde{\lambda}_i|)\rho|\tilde{\lambda}_i\rangle$) are bounded by the convergence value of the cost function $C(\boldsymbol{\beta}_{\mathrm{opt}})$:
    \begin{equation}
        \epsilon_\lambda, \, \epsilon_v \le \operatorname{Tr}[\rho^2] - \frac{(E_{m+1} - C(\boldsymbol{\beta}_{\mathrm{opt}}))^2}{\sum_{i=1}^m (E_{m+1} - E_i)^2}.
        \label{eq:operational_error_bound}
    \end{equation}
    Furthermore, an optional 2-copy verification protocol utilizing $b_m > m$ sampled bitstrings establishes an even tighter, hardware-verifiable bound:
    \begin{equation}
        \epsilon_\lambda, \, \epsilon_v \le \operatorname{Tr}[\rho^2] - \sum_{i=1}^{b_m} \tilde{\lambda}_i^2 - \frac{\left(1 - \sum_{i=1}^{b_m} \tilde{\lambda}_i\right)^2}{2^n - b_m}.
        \label{eq:verification_bound_2copy}
    \end{equation}
\end{theorem}

% ==============================================================================
\subsection{Comparative Resource Scaling: Why VQSE is Qubit-Frugal}
% ==============================================================================

Compared to existing quantum state diagonalization and principal component extraction algorithms, VQSE achieves ultimate qubit frugality by requiring only a \textbf{single copy of $\rho$ ($n$ qubits)} per circuit execution .

\begin{table}[h!]
    \centering
    \caption{Comparison of quantum state spectrum extraction algorithms for an $n$-qubit density matrix $\rho$.}
    \label{tab:vqse_comparison}
    \begin{tabular}{p{2.25cm} p{2.2cm} p{2.6cm} p{5.75cm}}
        \toprule
        \textbf{Algorithm} & \textbf{Qubit Count} & \textbf{Input State}               & \textbf{Primary NISQ Bottlenecks}                                                                                 \\
        \midrule
        \textbf{qPCA}      & $n + a$ qubits       & $\rho^{\otimes k}$ copies          & Requires Phase Estimation and matrix exponentiation $e^{-i\rho t}$; prohibitive circuit depth \cite{qpca_2014}.   \\
        \textbf{VQSD}      & $2n$ qubits          & $\rho \otimes \rho$ (2 copies)     & Requires joint 2-register gates; vulnerable to asymmetric inter-register noise \cite{LaRose2019_vqsd}.            \\
        \textbf{QSVD}      & $n$ to $2n$ qubits   & $|\psi_{AB}\rangle$ (Purification) & Requires explicit state purification; fails under uncharacterized incoherent noise \cite{Bravo_Prieto_2020_qsvd}. \\
        \textbf{VQSE}      & \textbf{$n$ qubits}  & \textbf{$\rho$ (1 copy)}           & \textbf{Single-copy execution, short depth, uses majorization for qubit frugality} \cite{cerezo2022vqse}.         \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{infobox}[label={sec:vqse_qng_optimization}]{Optimizing VQSE using Quantum Natural Gradient}
        The VQSE cost can be optimized with the quantum natural gradient (QNG), an optional variant of the baseline loop above. It minimizes $C(\boldsymbol{\beta}) = \operatorname{Tr}[H_{\mathrm{VQSE}} V(\boldsymbol{\beta})\rho V^\dagger(\boldsymbol{\beta})]$, where $H_{\mathrm{VQSE}}$ is a known non-degenerate Hamiltonian and $V(\boldsymbol{\beta})$ is a parameterized circuit \cite{cerezo2022vqse}. Standard gradient descent evaluates steepest descent in Euclidean parameter space, ignoring the local geometry of the ansatz-state manifold \cite{kolotouros2024RNG}.

        QNG replaces the Euclidean metric with the QFIM of the ansatz state. We denote this circuit-parameter metric by $F^{\mathrm{ans}}(\boldsymbol{\beta})$ to distinguish it from the QFIM of a sensed physical parameter; for a pure ansatz, it is four times the Fubini--Study metric \cite{gacon2021_perturbation_stochastic_qfi}.

        \subsubsection*{Methodology}

        Implementing QNG within the VQSE framework requires modifying the parameter update step. The procedure is formulated as follows:

        \begin{enumerate}
            \item \textbf{Gradient evaluation:} The first-order gradient of the VQSE cost function, $\nabla_{\boldsymbol{\beta}} C(\boldsymbol{\beta})$, is evaluated. For hardware-efficient ansatzes, this can be implemented with the parameter-shift rule \cite{cerezo2022vqse, kolotouros2024RNG}.
            \item \textbf{Ansatz-metric evaluation:} The ansatz QFIM, $F^{\mathrm{ans}}(\boldsymbol{\beta})$, captures the local geometry of the parameterized circuit state. Its elements are state-derivative overlaps and can require structural modifications to the circuit, such as a SWAP test or a compute--uncompute sequence \cite{kolotouros2024RNG, gacon2021_perturbation_stochastic_qfi}.
            \item \textbf{Parameter Update:} The classical optimizer updates the circuit parameters using the natural gradient descent rule:
                  \begin{equation}
                      \boldsymbol{\beta}_{k+1} = \boldsymbol{\beta}_k - \eta
                      \left(F^{\mathrm{ans}}(\boldsymbol{\beta}_k)+\lambda\mathbb{I}\right)^+
                      \nabla_{\boldsymbol{\beta}} C(\boldsymbol{\beta}_k),
                      \qquad \lambda\geq0,
                  \end{equation}
                  where $\eta$ represents the learning rate, $\lambda$ regularizes an ill-conditioned metric, and $+$ denotes the Moore--Penrose pseudoinverse \cite{kolotouros2024RNG}.
        \end{enumerate}

        \subsubsection*{Advantages and Limitations}

        The integration of QNG into VQSE yields several significant advantages. Primarily, QNG adjusts the step size based on the sensitivity of the quantum state to parameter variations, rather than employing a global learning rate. This dynamic scaling facilitates faster convergence in terms of optimization iterations and enables the algorithm to effectively navigate complex cost landscapes with high densities of local minima.

        However, exactly evaluating the ansatz QFIM poses severe scalability challenges for near-term quantum hardware. For an ansatz parameterized by $m$ variables, evaluating $F^{\mathrm{ans}}(\boldsymbol{\beta})$ requires $\mathcal{O}(m^2)$ distinct quantum state preparations per iteration \cite{kolotouros2024RNG, gacon2021_perturbation_stochastic_qfi}. Furthermore, measuring state overlaps inherently introduces circuit overhead, requiring either a doubling of the circuit width (e.g., using ancilla qubits for a SWAP test) or circuit depth, thereby exacerbating vulnerabilities to decoherence and gate infidelities. Finally, the pseudoinverse can be numerically unstable when the metric is ill-conditioned, motivating the regularization shown above \cite{gacon2021_perturbation_stochastic_qfi}.

        To mitigate these computational bottlenecks, stochastic approximations such as the Random Natural Gradient (RNG), Stochastic-Coordinate QNG, or Simultaneous Perturbation Stochastic Approximation (SPSA) can be substituted for the exact ansatz QFIM. These methods construct unbiased estimators or reduced-rank approximations using only $\mathcal{O}(1)$ or $\mathcal{O}(m)$ resources, drastically reducing the quantum overhead while preserving the geometric advantages of the natural gradient.
\end{infobox}

\begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{misc/sgd_vs_sr_comparison-set2-palette.png}
    \caption{Illustration of natural-gradient preconditioning in an independent TFIM wavefunction-learning example. The complex restricted-Boltzmann-machine calculation is not a VQSE benchmark; it illustrates why an ansatz-state metric can improve optimization relative to standard gradient descent.}
    \label{fig:sgd_vs_ng}
\end{figure}
